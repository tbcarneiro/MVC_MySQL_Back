@page
@model MVC_MySQL.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Seguro[0].NumApolice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seguro[0].CpfCnpj)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seguro[0].Placa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seguro[0].ValPremio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Seguro) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumApolice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CpfCnpj)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Placa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValPremio)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
